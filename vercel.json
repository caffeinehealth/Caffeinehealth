{
  "version": 2,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
{
  "version": 2,
  "builds": [
    {
      "src": "caffeine-health/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "caffeine-health/dist"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
{
  "version": 2,
  
  "routes": [
    { "src": "^/(.*)$", "dest": "/index.html" }
  ]
}
