{
  "version": 2,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
{
  "version": 2,
  
  "routes": [
    { "src": "^/(.*)$", "dest": "/index.html" }
  ]
}
